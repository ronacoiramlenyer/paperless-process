{
  "name": "paperless-process-gas",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "node build.mjs",
    "push": "npm run build && clasp push",
    "deploy": "npm run build && clasp push && clasp deploy"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "esbuild": "^0.24.0",
    "pdf-lib": "^1.17.1"
  }
}
